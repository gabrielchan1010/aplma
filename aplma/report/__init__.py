"""Colab-facing review report — runs the pipeline across multiple covenant
specs and renders one self-contained HTML report.

Public API:
    from aplma.report.review_session import build_sessions, ReviewSession
    from aplma.report.html_report import render_report
"""
